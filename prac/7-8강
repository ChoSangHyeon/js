오브젝트는 키와 벨류의 집합체이다.

오브젝트 내용을 닷으로 호출도 가능하고 (스트링타입)괄호에서 몽고 db 처럼 가져올수잇다.

주로 닷을 쓴다.
동적으로 값이 바뀔때 괄호로 호출

자료구조 

배열 array

const arr1 = new Array();
const arr2 = [1,2];

팝 푸시가 빠르니 좋다.

shift 와 unshift 는 pop(0) 느낌 

splice 배열의 중간값을 지운다.
splice()에서 원하는 인덱스에서 몇개지울지 설정 (설정안하면 전부 다 지워짐)
그리고 나중에 중간위치에 새 값을 추가도 가능하다


concat 은 배열의 합이다.

Searching 
검사 찾기
.indexof 인덱스호출 없으면 -1 호출(가장 첫번째 값)
.includes 포홤되있는지 확인 부울
.lastIndexof 마지막 인덱스 호출